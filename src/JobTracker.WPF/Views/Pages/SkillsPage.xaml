<Page x:Class="JobTracker.WPF.Views.Pages.SkillsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Skills" Background="{StaticResource BackgroundBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="🎓 Skills"
                   Style="{StaticResource PageTitleText}" Margin="0,0,0,16"/>

        <!-- Toolbar -->
        <Border Grid.Row="1" Style="{StaticResource ToolbarCard}" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource SearchBox}" Margin="0,0,8,0"/>
                <Button Grid.Column="1" Content="✏️ Edit"
                        Command="{Binding EditCommand}"
                        Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"/>
                <Button Grid.Column="2" Content="🗑 Delete"
                        Command="{Binding DeleteCommand}"
                        Style="{StaticResource DangerButton}" Margin="0,0,8,0"/>
                <Button Grid.Column="3" Content="➕ New Skill"
                        Command="{Binding NewCommand}"
                        Style="{StaticResource PrimaryButton}"/>
            </Grid>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0"
                      ItemsSource="{Binding FilteredSkills}"
                      SelectedItem="{Binding SelectedSkill}"
                      Style="{StaticResource MainDataGrid}"
                      ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                      AutoGenerateColumns="False" IsReadOnly="True"
                      SelectionMode="Single" GridLinesVisibility="Horizontal"
                      Margin="0,0,8,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Skill Name" Binding="{Binding Name}"     Width="*" MinWidth="140"/>
                    <DataGridTextColumn Header="Category"   Binding="{Binding Category}" Width="160"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Inline edit panel -->
            <Border Grid.Column="1" Width="300"
                    Style="{StaticResource FormCard}"
                    Visibility="{Binding IsFormVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel>
                    <TextBlock Text="Skill Details" Style="{StaticResource SectionHeader}" Margin="0,0,0,16"/>

                    <TextBlock Text="Name *" Style="{StaticResource FieldLabel}"/>
                    <TextBox Text="{Binding EditName, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource FormTextBox}" Margin="0,0,0,10"/>

                    <TextBlock Text="Category" Style="{StaticResource FieldLabel}"/>
                    <ComboBox Text="{Binding EditCategory, UpdateSourceTrigger=PropertyChanged}"
                              ItemsSource="{Binding ExistingCategories}"
                              IsEditable="True"
                              Style="{StaticResource FormCombo}" Margin="0,0,0,20"/>

                    <StackPanel Orientation="Horizontal">
                        <Button Content="💾 Save"
                                Command="{Binding SaveCommand}"
                                Style="{StaticResource PrimaryButton}" Margin="0,0,8,0"/>
                        <Button Content="Cancel"
                                Command="{Binding CancelCommand}"
                                Style="{StaticResource SecondaryButton}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Status bar -->
        <Border Grid.Row="3" Background="{StaticResource SurfaceBrush}" Padding="8,4" Margin="0,8,0,0">
            <Grid>
                <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource SubtleText}"/>
                <ProgressBar IsIndeterminate="True" Height="3" VerticalAlignment="Bottom"
                             Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"/>
            </Grid>
        </Border>
    </Grid>
</Page>
